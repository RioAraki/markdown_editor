# 首次上线验收 · 2026-09-15

正式入口为 http://192.168.71.10:3002。已从本机通过该局域网地址验证；没有将本机请求描述为另一台设备实测。

## 上线状态

- 发布版本：`20260915044815469-c97f59c8-4e226000`。
- editor 提交：`c97f59c8f7919f2519a8966b665d3ed3eb00a8dd`；shared 提交：`4e22600058d49879e70d3c40c12d3a4fc05ed57f`。
- Node v24.13.0 / Next 15.5.9。独立 release 已完成 npm ci、测试、生产构建；构建包含类型检查。
- Windows 任务 `MarkdownEditor Production` 正在运行，登录触发、每分钟补起、IgnoreNew、执行时限 PT0S。
- 原开发服务通过其独立控制台 CTRL_C 退出。04:51:59 UTC 开始切换事务，04:52:27 UTC 正式版通过健康检查；含退出旧 dev 的首次窗口约半分钟，未通过连续外部探针精确测量。
- 旧 `NextDevMaintain` 的 editor 条目已移除；WhatIf 只显示 diary:3001。旧脚本与任务 XML 保存在 runtime/backups/maintenance-handoff-20260915。
- 本地原分支已快进整合代码。开发启动命令使用本机 3003 和独立副本；验收后已关闭临时开发进程。候选 3004 已关闭。

## 验证结果

- 冻结 release：业务测试 91 通过；运行测试 65 通过、1 跳过、0 失败。跳过原因是当前 Windows 账户无法创建文件符号链接；目录 junction 隔离测试通过。
- 候选 3004：40 项 HTTP、资源与副本读写检查通过。
- 正式 3002：25 项只读检查通过，健康信息 mode=production、releaseId 正确，监听 0.0.0.0:3002。
- 开发 3003：37 项 HTTP、副本读写检查通过。启动、读写、关闭开发实例期间，正式进程 PID 5892 与 releaseId 保持不变。
- 故障行为使用隔离的真实子进程测试：退出、异步挂起、主线程阻塞、监督器退出、切换中断、启动失败恢复、重试限流、认证、请求排空、备份失败恢复和端口占用保护。
- 首次运行 RSS 约 142 MiB，记录口径为 Node RSS；这只是短时基线，不是长期内存上限。

## 数据和恢复证据

切换前备份：`D:/markdown_editor_runtime/backups/2026-09-15T04-51-59-553Z`。数据备份清单含源路径、大小及 SHA-256；私有配置另复制到该目录的 private-config，未进入源码库。

将备份恢复到 `D:/markdown_editor_runtime/implementation/restore-check-20260915`，2,005 个文件、137,015,481 字节逐项与清单匹配。演练没有覆盖正式文件。完整证据在 runtime/implementation/restore-evidence.json。

一次早期隔离测试误调用了正式 sync-repo，修改了 18 条既有记录的 redo 标记及说明。已保存前后文件、核对并恢复原始字节；证据在 runtime/backups/test-incident-20260915。测试随后改用临时 fixture，最终日记仓库仅保留用户原有日记修改。

备份停止本服务写入并复核源文件；不能冻结其他编辑器，也不是跨应用磁盘快照。部署和代码回滚不会恢复旧数据。

## 后续实际使用验证

- 另一台局域网设备访问、Windows 重新登录、关闭 Codex 后观察尚未实测。
- 48–72 小时及更长故障周期尚未经过；健康检查和日志已启用，但没有宣称长期稳定已验证。
- 首次正式发布 previous=null；下一次成功发布才产生可用的上一正式版本。失败回退状态机已通过隔离测试。

日常操作见 [runtime.md](runtime.md)。证据目录保留用于排查；其中包含私人数据，不应上传。
